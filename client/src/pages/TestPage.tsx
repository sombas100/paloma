import React from "react";
import CustomCarousel from "../Components/CustomCarousel";

const TestPage = () => {
  return (
    <div>
      <CustomCarousel />
    </div>
  );
};

export default TestPage;
